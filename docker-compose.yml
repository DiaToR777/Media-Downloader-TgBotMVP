services:
  bot:
    image: ghcr.io/diator777/media-downloader-tgbotmvp:latest
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_TG_ID=${ADMIN_TG_ID}
    volumes:
      - ./downloads:/app/downloads